DOM -- Document Object Model -- when web page is loaded  browser create a dom file of tht page
-- stored in tree structure (node) -- bridge bw HTML & JS

window -- default -- contain everything
console.log() //for dom tags
console.dir() //print object and documents

    TREE
WINDOW -- DOCUMENT(DOM) --HTML --1 BODY 2HEAD


    GetElementsByTagName    --- elements note s
It’s commonly used to select multiple elements like <p>, <li>, or even all elements (*) for manipulation
Store in the array index formate

    INBUILD
AddEventListener(event,function) --accept click() (event),onmouseover (event) ,functions (anonymous),

    EVENT
click ()  -- we need to click for event to work
onMouseOver () -- move the mouse over the data it will perform event -- till only curser on image
onmouseout () -- still reflect evn after the curser is out of image

